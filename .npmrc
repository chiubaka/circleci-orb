# Route @chiubaka scoped packages to GitHub Packages (org ADR 0034).
# The token is never committed; set GITHUB_TOKEN in your environment (see README).
@chiubaka:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
