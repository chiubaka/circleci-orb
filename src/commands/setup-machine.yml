description: >
  This command installs required dependencies for a machine executor.

parameters:
  node-version:
    type: string
    description: The full version tag of the NodeJS version to install.

steps:
  - node/install:
      node-version: << parameters.node-version >>
  - docker/install-dockerize
