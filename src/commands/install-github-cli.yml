description: >
  Ensures the GitHub CLI (gh) is available on PATH (installs via official script when missing).
  Use before changesets-release-pr or other steps that call gh.

steps:
  - run:
      name: Install GitHub CLI (gh)
      command: << include(scripts/installGithubCli.sh) >>
