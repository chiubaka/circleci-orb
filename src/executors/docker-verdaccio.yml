description: >
  Runs node and verdaccio on docker
docker:
  - image: cimg/node:<<parameters.tag>>
  - image: verdaccio/verdaccio
parameters:
  tag:
    default: lts
    description: >
      Pick a specific cimg/node image variant:
      https://hub.docker.com/r/cimg/node/tags
    type: string
